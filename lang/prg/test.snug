

use 'lib/block.snug';

rout main
{
    trans 10 ~ block;

    sub block::range (
        block ~ block,
        start ~ 0,
        end ~ 10
    );

    sub block::sum (
        block ~ block,
        sum ~ _
    );

    sub block::debug ( block ~ block );

    debug _;


}

